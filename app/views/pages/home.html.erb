<% if signed_in? %>
	<table class="front" summary="Pour visiteur identifie">
		<tr>
			<td class="main">
				<h1 class="micropost">Quoi de neuf?</h1>
				<%= render 'shared/micropost_form' %>
				<%= render 'shared/feed' %>
			</td>
			<td class="sidebar round">
				<%= render 'shared/user_info' %>
			</td>
		</tr>
	</table>
<% else %>
	<h1>Projet RoR Arnou Baptiste</h1>
	<p>
		Ceci est la page d'accueil du site representant le controle continu.
	</p>
	
	<%= link_to "S'inscrire!", signup_path, :class => "signup_button round"%>
<% end %>
